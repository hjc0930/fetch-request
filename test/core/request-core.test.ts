import { describe, expect, it } from "vitest";
import RequestCore from "../../src/core/RequestCore";

describe("RequestCore", () => {
  it("should be defined", () => {
    expect(RequestCore).toBeDefined();
  });
});
